{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "Database": {
    "MigrateOnStartup": true
  },

  "//": "Known demo accounts are permitted here and only here — CQ-017 allows them in explicitly local/development seed data. Never set this true for a deployed environment. The connection string still comes from environment configuration or user secrets, never from this file.",

  "AdminBootstrap": {
    "AllowDevelopmentDemoAccounts": true
  },

  "//DevelopmentAuth": "The dev-only authentication/authorization stubs (ST-002, ST-003) stand in for BL-002 and BL-007, which are not built yet. Permitted here and only here — Program.cs additionally requires IsDevelopment() before registering either, so this flag alone cannot open the door in another environment. Never set this true for a deployed environment.",

  "DevelopmentAuth": {
    "AllowDevelopmentAuthenticationStub": true
  }
}
